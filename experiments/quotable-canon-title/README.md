# Superseded prototype — do not tune this

This was the original standalone prototype for the floating-verses title
animation (full-field spawning, click-to-pop slips, word pile). It is kept as a
scrapbook only.

The **production version** lives in `assets/scripts/floating-verses.js` (calm
right-lane stream, PPA tricolor slips, attributions, no pops) and is used by
`decks/2026-06-poetic-canon/index.html`. Edit that, not this.
